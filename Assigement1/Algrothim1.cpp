#include <iostream>
#include<string>
using namespace std;


int main()
{
string a;
int i = 1, k , n;
bool found = false;
while (i <= n && found = false)
{
    if (a[i]= k )
    {
        found = true;
    };
    return found;
}
    return 0;
}